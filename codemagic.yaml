workflows:
  my-flutter-workflow:
    name: Flutter Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    scripts:
      - name: Build iOS App
        script: |
          flutter pub get
          flutter build ipa --release --no-codesign
    artifacts:
      - build/ios/ipa/*.ipa
